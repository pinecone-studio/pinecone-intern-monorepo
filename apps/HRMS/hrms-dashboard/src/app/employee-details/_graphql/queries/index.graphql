# write query
query GetAllEmployee {
  getAllEmployee {
    id
    imageUrl
    firstName
    lastName
    jobTitle
    email
    department
    employmentStatus
  }
}

query GetEmployeesByPaginate($paginationInput: PaginationInput!) {
  getEmployeesByPaginate(paginationInput: $paginationInput) {
    totalEmployees
    employees {
      id
      email
      firstName
      employmentStatus
      department
      imageUrl
      jobTitle
      ladderLevel
      lastName
    }
  }
}
