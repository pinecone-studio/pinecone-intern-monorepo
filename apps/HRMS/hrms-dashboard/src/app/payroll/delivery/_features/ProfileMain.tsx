import { PayrollButton } from '../_components';

export const PayrollMain = () => {
  return (
    <div>
      hello from Domain Payroll and Features Payroll main with
      <PayrollButton text={'Payroll button'} />
    </div>
  );
};
