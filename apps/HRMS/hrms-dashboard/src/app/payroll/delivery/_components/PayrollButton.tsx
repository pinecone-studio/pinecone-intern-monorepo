// type ProfileButtonProps = {
//   text: string;
// };

// export const PayrollButton: React.FC<ProfileButtonProps> = (props) => {
//   const { text } = props;
//   return <button>{text}</button>;
// };
