import { ArticlesButton } from '../_components';

export const ArticlesMain = () => {
  return (
    <div>
      hello from Domain Articles and Features Articles main with
      <ArticlesButton text={'Articles button'} />
    </div>
  );
};
