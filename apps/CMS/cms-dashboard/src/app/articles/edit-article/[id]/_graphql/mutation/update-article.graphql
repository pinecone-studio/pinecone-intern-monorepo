mutation UpdateArticle($id: ID!, $title: String!, $content: String!, $category: ID!, $commentPermission: Boolean!) {
  updateArticle(_id: $id, title: $title, content: $content, category: $category, commentPermission: $commentPermission) {
    id
    title
    content
    commentPermission
    category {
      name
    }
  }
}
