mutation UpdateArticle($id: ID!, $title: String!, $content: String!, $commentPermission: Boolean!) {
  updateArticle(_id: $id, title: $title, content: $content, commentPermission: $commentPermission) {
    content
    id
    title
    commentPermission
  }
}
