query GetComments($input: GetCommentsLimitInput!) {
  getComments(input: $input) {
    comments {
      email
      name
      status
      createdAt
      comment
      articleId
      _id
    }
    allCount
    count
    deletedCount
    hiddenCount
    normalCount
  }
}
