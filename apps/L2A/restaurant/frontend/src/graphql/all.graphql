query Query {
  sampleQuery
}

mutation Mutation {
  sampleMutation
}
mutation AddTable($input: AddTableInput!) {
  addTable(input: $input) {
    _id
    name
    qrCodeUrl
    createdAt
    updatedAt
  }
}
