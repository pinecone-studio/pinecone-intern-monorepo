query GetUsers {
  getUsers {
    _id
    userName
    isPrivate
    profileImage
    bio
  }
}

mutation Login($input: LoginInput!) {
  login(input: $input) {
    token
    user {
      id: _id
      email
    }
  }
}
