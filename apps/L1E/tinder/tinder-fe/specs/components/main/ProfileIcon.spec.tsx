import { ProfileIcon } from '@/components/main/ProfileIcon';
import { render } from '@testing-library/react';

describe('Profile Icon render', () => {
  it('should call render successfully', () => {
    render(<ProfileIcon />);
  });
});
