import { render } from '@testing-library/react';
import RequestcomPaid from '@/components/requestForm/RequestFormPaid';

describe('RequestcomPaid', () => {
  it('should RequestcomPaid', async () => {
    render(<RequestcomPaid />);
  });
});
