import { render } from '@testing-library/react';
import Requestcomremote from '@/components/requestForm/RequestFormRemote';

describe('Requestcomremote', () => {
  it('should Requestcomremote', async () => {
    render(<Requestcomremote />);
  });
});
