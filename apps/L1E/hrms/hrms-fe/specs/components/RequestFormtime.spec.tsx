import { render } from '@testing-library/react';
import RequestcomTime1 from '@/components/requestForm/RequestFormtime';

describe('RequestcomTime1', () => {
  it('should RequestcomTime1', async () => {
    render(<RequestcomTime1 />);
  });
});
