mutation CreatePost($input: CreatePostInput!) {
  createPost(input: $input) {
    _id
    userId {
      username
      _id
      profilePicture
    }
    images
    caption
    createdAt
    updatedAt
  }
}
