import { CheckingBookingRight } from '@/components/main';
import '@testing-library/jest-dom';
import { render } from '@testing-library/react';

describe('Main CheckingBookingRight', () => {
  it('should render the main CheckingBookingRight', () => {
    render(<CheckingBookingRight />);
  });
});
