import { DetailsRight } from '@/components/admin/assets';
import '@testing-library/jest-dom';
import { render } from '@testing-library/react';

describe('Admin Details Right', () => {
  it('should render the admin details right', () => {
    render(<DetailsRight />);
  });
});
