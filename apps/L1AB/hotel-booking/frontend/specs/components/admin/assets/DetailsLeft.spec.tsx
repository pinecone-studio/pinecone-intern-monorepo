import { DetailsLeft } from '@/components/admin/assets';
import '@testing-library/jest-dom';
import { render } from '@testing-library/react';

describe('Admin Details Left', () => {
  it('should render the admin details left', () => {
    render(<DetailsLeft />);
  });
});
