'use client';
import { AdminDash } from '@/components';
import { Container } from '@/components';

const Page = () => {
  return (
    <Container>
      <AdminDash />
    </Container>
  );
};

export default Page;
