import { Container } from '@/components';
import { BookTicket } from '@/components/BookTicket';
import React from 'react';

const Page = () => {
  return (
    <Container>
      <BookTicket />
    </Container>
  );
};

export default Page;
