query Query {
  sampleQuery
}

mutation Mutation {
  sampleMutation
}

mutation CreateEvent($input: EventInput!) {
  createEvent(input: $input) {
    _id
    name
    artistName
    description
    eventDate
    eventTime
    images
    venues {
      name
      quantity
      price
    }
    discount
  }
}
