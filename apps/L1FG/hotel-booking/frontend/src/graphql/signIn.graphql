mutation SignIn($input: SignInInput!) {
  signIn(input: $input) {
    user {
      firstName
      lastName
      birthDate
      email
      phoneNumber
      emergencyContact
      status
      password
      otp
    }
    token
  }
}