import { BluePlus } from '@/components/admin/svg';
import '@testing-library/jest-dom';
import { render } from '@testing-library/react';

describe('BluePlus', () => {
  it('should render BluePlus successfully', async () => {
    render(<BluePlus />);
  });
});
