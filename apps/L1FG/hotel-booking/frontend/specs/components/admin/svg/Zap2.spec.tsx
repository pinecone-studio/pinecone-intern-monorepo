import { Zap } from '@/components/admin/svg';
import '@testing-library/jest-dom';
import { render } from '@testing-library/react';

describe('Zap', () => {
  it('should render Zap successfully', async () => {
    render(<Zap />);
  });
});
