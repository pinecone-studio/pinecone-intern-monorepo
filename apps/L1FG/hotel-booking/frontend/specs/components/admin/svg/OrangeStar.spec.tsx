import { OrangeStar } from '@/components/admin/svg';
import '@testing-library/jest-dom';
import { render } from '@testing-library/react';

describe('OrangeStar', () => {
  it('should render OrangeStar successfully', async () => {
    render(<OrangeStar />);
  });
});
