import { Plus } from '@/components/admin/svg';
import '@testing-library/jest-dom';
import { render } from '@testing-library/react';

describe('Plus', () => {
  it('should render Plus successfully', async () => {
    render(<Plus />);
  });
});
