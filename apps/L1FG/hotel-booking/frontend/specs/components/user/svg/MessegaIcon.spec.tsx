import { MessegaIcon } from '@/components/user/svg/MessegaIcon';
import '@testing-library/jest-dom';
import { render } from '@testing-library/react';

describe('MessegaIcon', () => {
  it('should render successfully', async () => {
    render(<MessegaIcon />);
  });
});
