import { BlueDital } from '@/components/user/ui/dital';
import '@testing-library/jest-dom';
import { render } from '@testing-library/react';

describe('BlueDital', () => {
  it('should render BlueDital successfully', async () => {
    render(<BlueDital />);
  });
});
