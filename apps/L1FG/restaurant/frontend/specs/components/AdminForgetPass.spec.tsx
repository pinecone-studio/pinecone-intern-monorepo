import AdminForgetPass from '@/components/admin-profile/AdminForgetPass';
import { render } from '@testing-library/react';

describe('AdminForgetPass', () => {
  it('AdminForgetPass', async () => {
    render(<AdminForgetPass />);
  });
});
