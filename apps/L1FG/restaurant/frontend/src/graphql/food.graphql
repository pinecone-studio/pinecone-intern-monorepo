query GetFoods {
  getFoods {
    id
    foodName
    imageUrl
    price
    status
    categoryId
    createdAt
  }
}

 mutation CreateFood($input: FoodInputType!) {
   createFood(input: $input) {
     foodName
     imageUrl
     price
     status
   }
 }