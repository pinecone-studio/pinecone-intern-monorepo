import { render } from '@testing-library/react';
import { InstagramIcon } from '@/components/layout/icons/InstagramIcon';

describe('InstagramIcon', () => {
  it('should render successfully', () => {
    render(<InstagramIcon />);
  });
});
