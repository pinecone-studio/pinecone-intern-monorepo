import { render } from '@testing-library/react';
import { PhoneIcon } from '@/components/layout/icons/PhoneIcon';

describe('CustomerSupportIcon', () => {
  it('should render successfully', () => {
    render(<PhoneIcon />);
  });
});
