import { render } from '@testing-library/react';
import { HouseIcon } from '../../../../src/components/layout/icons/HouseIcon';

describe('HouseIcon', () => {
  it('should render successfully', () => {
    render(<HouseIcon />);
  });
});
