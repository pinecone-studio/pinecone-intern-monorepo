import { render } from '@testing-library/react';
import { YoutubeIcon } from '@/components/layout/icons/YoutubeIcon';

describe('YoutubeIcon', () => {
  it('should render successfully', () => {
    render(<YoutubeIcon />);
  });
});
