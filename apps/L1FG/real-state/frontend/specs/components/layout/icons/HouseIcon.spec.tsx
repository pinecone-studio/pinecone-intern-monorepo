import { HouseIcon } from '@/components/layout/icons/HouseIcon';
import { render } from '@testing-library/react';

describe('it should render successfully', () => {
  it('should render successfully', () => {
    render(<HouseIcon />);
  });
});
