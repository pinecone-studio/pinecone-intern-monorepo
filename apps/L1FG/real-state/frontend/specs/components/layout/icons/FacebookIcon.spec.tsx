import { render } from '@testing-library/react';
import { FacebookIcon } from '@/components/layout/icons/FacebookIcon';

describe('FacebookIcon', () => {
  it('should render successfully', () => {
    render(<FacebookIcon />);
  });
});
