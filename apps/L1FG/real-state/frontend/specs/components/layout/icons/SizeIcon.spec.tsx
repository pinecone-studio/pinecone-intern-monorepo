import { SizeIcon } from '@/components/layout/icons/SizeIcon';
import { render } from '@testing-library/react';

describe('SizeIcon', () => {
  it('should render successfully', () => {
    render(<SizeIcon />);
  });
});
