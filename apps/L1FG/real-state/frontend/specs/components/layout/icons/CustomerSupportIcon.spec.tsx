import { render } from '@testing-library/react';
import { CustomerSupportIcon } from '@/components/layout/icons/CustomerSupportIcon';

describe('CustomerSupportIcon', () => {
  it('should render successfully', () => {
    render(<CustomerSupportIcon />);
  });
});
