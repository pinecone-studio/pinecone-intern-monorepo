import { AdminOwnerPost } from '@/components/adminPage/AdminOwnerPost';
import { render } from '@testing-library/react';

describe('AdminOwnerPost', () => {
  it('should render successfully', () => {
    render(<AdminOwnerPost />);
  });
});
