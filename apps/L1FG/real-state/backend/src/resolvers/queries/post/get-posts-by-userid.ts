// import { QueryResolvers } from '../../../generated';
// import { Owner } from '../../../models/owner-model';

// export const getPostsByUserId: QueryResolvers['getPostsByUserId'] = async (_, { input = {} }) => {
//   const { propertyOwnerId } = input;
//   const userPosts = await Owner.find({ propertyOwnerId });

//   return userPosts;
// };
