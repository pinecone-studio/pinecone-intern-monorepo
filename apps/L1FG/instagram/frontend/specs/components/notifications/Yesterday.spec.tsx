import { Yesterday } from '@/components/notifications/Yesterday';
import { render } from '@testing-library/react';
describe('Profile', () => {
  it('Should render', () => {
    render(<Yesterday />);
  });
});
