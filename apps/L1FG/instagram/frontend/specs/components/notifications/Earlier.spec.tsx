import { Earlier } from '@/components/notifications/Earlier';
import { render } from '@testing-library/react';
describe('Profile', () => {
  it('Should render', () => {
    render(<Earlier />);
  });
});
