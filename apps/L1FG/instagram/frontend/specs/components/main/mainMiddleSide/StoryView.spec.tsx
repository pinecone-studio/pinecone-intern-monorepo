import MainPageStory from '@/components/Home/mainBar/MainPageStory';
import { render } from '@testing-library/react';
describe('MenuButtons', () => {
  it('Should render', () => {
    render(<MainPageStory />);
  });
});
