import Tagged from '@/components/profile/profilePost/Tagged';
import { render } from '@testing-library/react';

describe('Tagged Component', () => {
  it('render Tagged component', () => {
    render(<Tagged />);
  });
});
