import Tagged from '@/components/profile/post/Tagged';
import { render } from '@testing-library/react';

describe('Tagged Component', () => {
  it('render Tagged component', () => {
    render(<Tagged />);
  });
});
