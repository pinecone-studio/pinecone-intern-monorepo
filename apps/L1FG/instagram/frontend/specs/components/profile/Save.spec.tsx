import Save from '@/components/profile/Save';
import { render } from '@testing-library/react';

describe('Save Component ', () => {
  it('render Save component', () => {
    render(<Save />);
  });
});
