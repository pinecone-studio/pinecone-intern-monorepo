import Save from '@/components/profile/post/Save';
import { render } from '@testing-library/react';

describe('Save Component ', () => {
  it('render Save component', () => {
    render(<Save />);
  });
});
