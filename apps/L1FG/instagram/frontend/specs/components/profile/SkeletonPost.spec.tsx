import SkeletonPost from '@/components/profile/profilePost/SkeletonPost';
import { render } from '@testing-library/react';

describe('it should render successfully', () => {
  it('should render successfully', () => {
    render(<SkeletonPost />);
  });
});
