mutation SignUp($input: UserInput!) {
  signUp(input: $input) {
    _id
    password
    email
  }
}
mutation SignIn($input: UserInput!) {
  signIn(input: $input) {
    user {
      _id
      password
      email
    }
    token
  }
}
