query GetLessons($courseId: ID!) {
  getLessons(courseId: $courseId) {
    id
    courseId {
      id
      title
      thumbnail
      content
    }
    title
    thumbnail
    content
  }
}

mutation CreateLesson($createInput: CreateLessonInput) {
  createLesson(createInput: $createInput) {
    id
    courseId {
      id
      title
      thumbnail
      content
    }
    title
    thumbnail
    content
  }
}
