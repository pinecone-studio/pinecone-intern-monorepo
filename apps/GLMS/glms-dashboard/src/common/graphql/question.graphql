mutation CreateQuestion($createInput: CreateQuestionInput!) {
  createQuestion(createInput: $createInput) {
    id
    quizId {
      id
      courseId
    }
    text
    options {
      id
      optionText
      isCorrect
    }
  }
}