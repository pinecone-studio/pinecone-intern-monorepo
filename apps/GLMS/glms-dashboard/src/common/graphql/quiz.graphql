mutation CreateQuiz($createInput: CreateQuizInput!) {
  createQuiz(createInput: $createInput) {
    id
    courseId
  }
}

query GetQuiz($courseId: ID!) {
  getQuiz(courseId: $courseId) {
    id
    courseId
  }
}