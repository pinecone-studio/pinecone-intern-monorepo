mutation CreateCourse($content: [ContentInput]!, $title: String!, $thumbnail: String!, $position: Int) {
  createCourse(content: $content, title: $title, position: $position) {
    _id
    title
    content {
      title
      description
    }
  }
}
