import { ProfileButton } from '../_components';

export const ProfileMain = () => {
  return (
    <div>
      hello from Domain Profile and Features Profile main with
      <ProfileButton text={'profile button'} />
    </div>
  );
};
