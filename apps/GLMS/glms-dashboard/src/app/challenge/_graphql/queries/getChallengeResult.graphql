query GetChallengeResult($courseId: String) {
  getChallengeById(courseId: $courseId) {
    _id
    author
    quiz {
      _id
      choices {
        _id
        choice
        isCorrect
      }
      choicesType
      question
    }
    courseId
    status
    title
  }
}
