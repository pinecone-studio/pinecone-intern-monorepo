query GetChallenge($challengeId: ID) {
  getChallengeById(challengeId: $challengeId) {
    _id
    author
    quiz {
      _id
      choices {
        _id
        choice
      }
      choicesType
      question
    }
    refCourse
    status
    title
  }
}
