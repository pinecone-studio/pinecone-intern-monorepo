query GetChallenge($courseId: String) {
  getChallengeById(courseId: $courseId) {
    _id
    author
    quiz {
      _id
      choices {
        _id
        choice
      }
      choicesType
      question
    }
    courseId
    status
    title
  }
}
