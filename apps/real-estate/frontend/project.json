{
  "name": "real-estate",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/real-estate/frontend",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/real-estate/frontend"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/real-estate/frontend"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "real-estate:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "real-estate:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "real-estate:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "real-estate:build:production"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/real-estate/frontend/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/real-estate/frontend/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
