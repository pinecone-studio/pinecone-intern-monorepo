query Query {
  sampleQuery
}

mutation Mutation {
  sampleMutation
}

mutation LoginUser($input: LoginInput!) {
  loginUser(input: $input) {
    user {
      _id
      username
      email
      createdAt
      updatedAt
    }
    sessionToken
  }
}

