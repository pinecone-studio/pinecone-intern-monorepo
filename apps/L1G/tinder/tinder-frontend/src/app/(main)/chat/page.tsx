'use client';
import ChatPage from '@/components/ChatPage';
import { Header } from '@/components/Header';

const Chat = () => {
  return (
    <div>
      <Header />
      <ChatPage />
    </div>
  );
};

export default Chat;
