'use client';
import HomePageContainer from '@/components/home/HomePageContainer';
import { Navbar } from '@/components/Navbar';

const Home = () => {
  return (
    <div>
      <Navbar />
      <HomePageContainer />
    </div>
  );
};

export default Home;
