import { SheetMenuShop } from '@/components/SheetMenuShop';
import { render } from '@testing-library/react';

describe('aboutus page', () => {
  it('should render the sheetMenu Component', () => {
    render(<SheetMenuShop />);
  });
});
