overwrite: true
schema: './<%= directory %>/src/graphql/schema'

generates:
  ./<%= directory %>/src/graphql/generated/index.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      contextType: Context
      makeResolverTypeCallable: true
      maybeValue: T
