{
    "properties": {
        "inputCsvPath": {
            "type": "string",
            "description": "File path of the input CSV file."
        },
        "outputSqlPath": {
            "type": "string",
            "description": "File path where the SQL file will be saved."
        },
        "tableName": {
            "type": "string",
            "description": "The name of the SQL table to which the data will be inserted."
        }
    },
    "required": [
        "inputCsvPath",
        "outputSqlPath",
        "tableName"
    ],
    "type": "object",
    "description": "Options required to convert a CSV file to SQL insert statements."
}